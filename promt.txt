generate the complete code for the following files based on the schema and requirements above. Follow the existing project coding style (PDO for database, require_once for views).

models/Siswa.php:

Class name: Siswa

Methods: getAll(), getById($id), create($data), update($id, $data), delete($id).

Note: Since there are foreign keys (id_kelas, id_spp), for now just treat them as standard inputs.

controllers/SiswaController.php:

Class name: SiswaController

Methods: index(), create(), edit($id), delete($id).

Ensure all redirects use index.php?controller=siswa&action=index.

views/siswa/index.php:

Display a table of students.

"Add New" button must link to index.php?controller=siswa&action=create.

Edit/Delete links must include controller=siswa.

views/siswa/create.php and views/siswa/edit.php:

Forms must point to action="index.php?controller=siswa&action=...".

Snippet for views/layouts/sidebar.php:

Provide the HTML <a> tag to add to the sidebar menu.

It must use href="index.php?controller=siswa&action=index".

Include the Router::isActive('index', 'siswa') check for the active class.

Files for Context (Use existing style):

Reference config/Database.php for connection.

Reference views/layouts/header.php, sidebar.php, footer.php for layout structure.